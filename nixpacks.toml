[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = [
  "pnpm install --frozen-lockfile"
]

[phases.build]
cmds = [
  "pnpm --filter @padel/types build",
  "pnpm --filter @padel/api build",
  "cd apps/api && pnpm prisma generate"
]

[start]
cmd = "cd apps/api && pnpm start"

